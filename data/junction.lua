-- The purpose of this file is just to allow us to refer to tuning.lua from both IC and hacked combotest;
-- hence, combotest's operation relies on a symbolic link being set up in ImpossibleCreatures/data called
-- "junction.lua" which links to tellurian/data/tuning.lua.
dofilepath("data:sigma/tuning.lua")